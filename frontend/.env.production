# Production build target. Same-origin /proxy path is rewritten to the Render
# backend at the Vercel edge, so the browser never makes a cross-origin call
# and the backend CORS allowlist (localhost only) stops mattering.
VITE_API_URL=/proxy
